<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="newhome.baselibrary">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".Share.MyShareActivity"/>
        <activity android:name=".Pay.PayMentActivity"></activity>
    </application>

</manifest>
